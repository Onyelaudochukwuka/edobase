import React, { useState } from 'react';

import type { NextPage } from 'next';

import { Layout } from '../components';

const Home: NextPage = () => {
  return (
    <Layout>
      <div>Details</div>
    </Layout>
  );
};

export default Home;
